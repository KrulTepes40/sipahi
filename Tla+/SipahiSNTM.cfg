SPECIFICATION Spec

(* Terminal state (all tasks Isolated) is acceptable — kernel halts gracefully. *)
CHECK_DEADLOCK FALSE

CONSTANTS
    Tasks = {t0, t1}
    ReservedLowEntries = 8
    MaxPmpEntries = 16

INVARIANT
    TypeOK
    KernelPmpInvariant
    UModeRequiresDispatch
    NoIsolatedRunning
    AtMostOneRunning
    RunningIsCurrent
