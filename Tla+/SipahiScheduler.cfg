SPECIFICATION Spec

CHECK_DEADLOCK
FALSE

CONSTANTS
    TASKS = {0, 1}
    MAX_PRIORITY = 2
    MAX_PERIOD = 3

INVARIANT
    TypeOK
    AtMostOneRunning
    RunningHasHighestPriority
    BudgetNonNegative

\* NOT: StarvationFreedom fixed-priority preemptive'de tutmaz —
\* DAL-A hep çalışır, DAL-D starve edilebilir (design intent).
\* Bu design-incorrect property, verify edilmez.
\* U-18 GÖREV 5: IsolatedNeverScheduled INVARIANT → PROPERTY
\* (temporal permanence: state="Isolated" => [](state="Isolated")).
PROPERTY
    DeadNeverScheduled
    IsolatedNeverScheduled
    NoDeadlock
